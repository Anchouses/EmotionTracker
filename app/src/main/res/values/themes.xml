<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EmotionTracker" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/frame</item>
        <item name="android:statusBarColor">@color/frame</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:typeface">normal</item>
        <item name="android:letterSpacing">0.015</item>
    </style>

    <style name="Theme.EmotionTracker" parent="Base.Theme.EmotionTracker" />
</resources>
